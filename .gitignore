# Secrets — NE JAMAIS committer
.env
service_account.json

# Environnement Python
venv/
__pycache__/
*.pyc

# Logs
*.log

# Base de données (état runtime, persiste sur disque hors git)
trading.db
trading.db-shm
trading.db-wal
trading.db.bak-*

# État runtime du forward-test (portefeuille virtuel, persiste hors git)
forward_state.json
forward_state.json.tmp

# Sortie watcher
trade_watcher.py
